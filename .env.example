# OAuth 1.0a user-context credentials for the sending account.
# Required privileges depend on the action:
#   DMs / welcome / media     — DM write access
#   Feedback cards            — feedback_api client privilege
#                               + feedback_api_access_create role on the user
#   Custom profiles           — verified + allowlisted account

X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
